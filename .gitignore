# Dependencies
node_modules/

# Environment and secrets — NEVER commit
.env
.env.*
!.env.example
docs/architecture/credentials/

# Build output
dist/
build/
.wrangler/

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Data output (committed via pipeline, not manually)
# data/ is intentionally tracked — pipeline commits snapshots

# Large generated FAOSTAT backfill (re-created by `bun run pipeline:prefetch`)
data/reference/faostat-pp-backfill.json

# Local landing backup (Phase 3 rollback artifact — not for publication)
landing-backup-pre-phase3-2026-05-27/
