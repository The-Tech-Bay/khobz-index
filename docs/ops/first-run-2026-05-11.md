# First KKI weekly pipeline verification (§3.8B.8)

**Date:** 2026-05-11  
**Environment:** local developer machine (`bun run pipeline`); GitHub Actions workflow added at repo root `.github/workflows/kki-weekly.yml` (dispatch after merge + secrets).

## Results

| Step | Status | Notes |
|------|--------|--------|
| Pipeline (`bun run pipeline`) | OK | FAO FPI adapter invoked; `calculateKKI` for **MA**; `persistCountryMonth` + APK bundle; **5** R2 keys mirrored under `build/r2-mirror/`. |
| Adapters (stub) | Partial | Only **`fao-fpi`** participates in this stub; other sources left **`up`** (not evaluated) — see `pipeline-run-summary.json`. |
| Engine ≥1 country | OK | **MA** index record in rollup + storage mirror. |
| R2 snapshot (local) | OK | Mirror written to `build/r2-mirror/` (e.g. `v1.0/MA/2026-04.json`, `manifest.json`, bundle). |
| R2 / KV in production | Pending human | Requires `wrangler login`, `scripts/cf-bootstrap.sh`, and GitHub secrets — see [runbook.md](./runbook.md). |
| GitHub Release | Pending human | Run **`kki-weekly`** via Actions after configuring repo; **softprops/action-gh-release** publishes `build/khobz-index-*.{json,csv}`. |
| IPFS pin | Optional | Set `PINATA_JWT` + use archive path (§3.6B); weekly job does not block on IPFS. |
| `GET /health` fresh data | Pending human | After weekly job writes **`pipeline:status`** to KV, Worker `/health` shows `last_successful_run_at` / `last_run_week_id`. |
| Karama backend closed API | Pending human | Validate **`POST /auth/exchange`** + **`GET /kki/latest/MA`** against deployed Worker with a real Supabase JWT. |

## Pilot country (minimum)

- **MA:** KKI computed and persisted in stub rollup (see `build/khobz-index-2026-04.json` — month follows `PIPELINE_MONTH` / previous UTC month rule).

## Artifacts (local)

- `build/khobz-index-YYYY-MM.json` + `.csv`
- `build/pipeline-run-summary.json` — adapter metadata, `week_id`, `sources`, `degraded_source_count`
- `build/pipeline-kv-pipeline-status.json` — payload for **`wrangler kv key put pipeline:status`**
- `build/last-run-state.json` — includes `kv_pipeline` + summary (for `state/last-run.json` on R2)
- `build/r2-mirror/**` — tree to sync with **`wrangler r2 object put`**

## Observability

- API integration tests emit **`kki_observability`** JSON logs (latency, status, path) — see `src/api/middleware/observability.ts`.

---

*CI wiring + Cloudflare login are the remaining steps to close production DoD.*
